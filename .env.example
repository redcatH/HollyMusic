# Database URL - 在 Docker 中使用相对路径
DATABASE_URL="file:./prisma/data/music.db"

# 文件缓存控制（可选）
ENABLE_FILE_CACHE=false  # 默认开启，设为 false 可关闭本地音频文件缓存

# 其他环境变量可在这里添加
# 例如：
# NODE_ENV=production
# LOG_LEVEL=info
