/**
 * 推荐任务的默认提示词（前后端共享，纯字符串常量无副作用）。
 * 前端创建表单预填用，engine 跑时若 config 未提供则用此默认值。
 *
 * user 模板用 {{artist}} {{candidates}} 占位符，engine 每歌手替换。
 */

export const DEFAULT_PROMPT_SYSTEM = '你是音乐曲库编辑。只返回 JSON, 不输出任何额外文字。'

export const DEFAULT_PROMPT_USER = `为目标歌手「{{artist}}」筛选值得推荐的版本——以本人演唱为主, 兼顾广受喜爱的经典翻唱。

候选(序号. 歌名 | 演唱者 | 专辑):
{{candidates}}

规则:
1. 保留「{{artist}}」本人主唱的版本, 包括其翻唱/重新演绎的经典版本——不必是原唱, 只要广受欢迎即可。
2. 排除演唱者不是「{{artist}}」本人的版本(群星/大合唱/晚会/他人翻唱其作品)。
3. 排除 live/现场版(层次不齐, 一律不要)、DJ版/remix/混音/伴奏/纯音乐/铃声/8bit 等非录音室版本。
4. 同名重复只留 1 个最受欢迎、最经典的录音室版本。
5. 演唱者里的英文名/别名/组合名变体视为同一人。

严格返回 JSON: {"selected":[序号按推荐度降序],"dropped":{"被排除序号":"≤8字原因"}}`

// 按歌曲：一首歌搜出多个版本(原唱/翻唱/live/remix/群星…), 从中挑大众认可的好版本, 每首最多 2 个
export const DEFAULT_PROMPT_USER_FOR_SONGS = `为目标歌曲「{{song}}」从下列多个版本中挑出大众认可的好版本——以经典原唱录音室版为主, 兼顾公认超越原唱的经典翻唱与官方经典 remix/混音版。

候选(序号. 歌名 | 演唱者 | 专辑):
{{candidates}}

规则:
1. 优先保留该歌曲的「原唱/最经典录音室版本」(通常是最广为人知的那个)。
2. 若存在公认艺术成就或传唱度超越原唱的经典翻唱(如邓丽君/张国荣等经典演绎), 或官方正式发行且大众认可度高的经典 remix/混音版, 可一并保留, 每首最多 2 个版本。
3. 排除 live/现场版/演唱会(层次不齐, 一律不要)、伴奏/纯音乐/铃声/8bit/短视频剪辑等非正式录音室版本。
4. 排除粗制 DJ 舞曲版/低质混音、「群星/大合唱/晚会/合集」类演唱者, 以及明显蹭歌名的同名异曲。
5. 演唱者里的英文名/别名/组合名变体视为同一人; 同一版本只留 1 条。

严格返回 JSON: {"selected":[序号按推荐度降序],"dropped":{"被排除序号":"≤8字原因"}}`

// ============ AI 辅助筛选（推荐管理面板：辅助推荐 / 辅助取消）============
// 占位符: {{candidates}} 候选列表; {{userPrompt}} 用户补充需求（可空）
// 返回: {"suggestions":[{"index":序号,"action":"keep"|"remove","reason":"≤20字原因"}]}

// 辅助推荐：从搜索候选里挑值得加入白名单的
export const DEFAULT_PROMPT_AI_ADD = `你是音乐曲库编辑。从下列候选歌曲中挑出值得加入推荐白名单的版本。

候选(序号. 歌名 | 演唱者 | 专辑):
{{candidates}}

用户补充需求（可空）: {{userPrompt}}

规则:
1. 保留大众认可、传唱度高的好版本（经典原唱录音室版优先，公认经典翻唱/官方经典 remix 可保留）。
2. 排除 live/现场版、伴奏/纯音乐/铃声/8bit、粗制 DJ 舞曲版、群星/大合唱/晚会、蹭歌名同名异曲。
3. 同名重复只留最经典的 1 个。
4. 对每条候选给出 keep（建议加入）或 remove（不建议）及简短原因。

严格返回 JSON: {"suggestions":[{"index":序号,"action":"keep"|"remove","reason":"≤20字原因"}]}`

// 辅助取消：从白名单里挑可以取消推荐的
export const DEFAULT_PROMPT_AI_REMOVE = `你是音乐曲库编辑。从下列已推荐歌曲中挑出可以取消推荐的版本。

候选(序号. 歌名 | 演唱者 | 专辑):
{{candidates}}

用户补充需求（可空）: {{userPrompt}}

规则:
1. 建议取消（remove）：冷门/低质/重复版本、live/现场/伴奏/铃声、粗制 remix、群星合唱、与白名单主题不符的。
2. 建议保留（keep）：经典原唱、公认经典翻唱、高传唱度版本。
3. 对每条候选给出 keep（建议保留）或 remove（建议取消）及简短原因。

严格返回 JSON: {"suggestions":[{"index":序号,"action":"keep"|"remove","reason":"≤20字原因"}]}`

// ============ AI 协助生成名单（推荐任务面板）============
// 占位符: {{userPrompt}} 用户需求描述
// 返回: {"items":["名称1","名称2",...]}

export const DEFAULT_PROMPT_AI_GENERATE_ARTISTS = `你是音乐曲库编辑。根据用户需求生成一批歌手名单。

用户需求: {{userPrompt}}

规则:
1. 生成 10-30 个符合需求的歌手名（中文名为主，可含知名英文名）。
2. 每行一个，去重，不重复。
3. 只返回歌手名，不带任何说明或编号。

严格返回 JSON: {"items":["歌手1","歌手2",...]}`

export const DEFAULT_PROMPT_AI_GENERATE_SONGS = `你是音乐曲库编辑。根据用户需求生成一批歌曲名单。

用户需求: {{userPrompt}}

规则:
1. 生成 10-30 首符合需求的歌曲名（中文歌名为主）。
2. 每行一个，去重，不重复。
3. 只返回歌曲名，不带演唱者、说明或编号。

严格返回 JSON: {"items":["歌曲1","歌曲2",...]}`

// ============ AI 协助创建歌单（playlist-assist，面向所有登录用户）============
// 占位符: {{userPrompt}} 用户需求描述
// 返回: {"mode":"songs"|"artists","items":[...],"playlistName":"..."}
export const DEFAULT_PROMPT_AI_PLAYLIST_GENERATE = `你是音乐曲库编辑。根据用户需求, 为其规划一份歌单的选曲方向。

用户需求: {{userPrompt}}

你的任务:
1. 先判断这个需求更适合「按具体歌曲」还是「按歌手」选曲。
   - 适合按歌曲(mode=songs): 需求指向特定曲目、曲风、场景、情绪、年代、主题(如"适合深夜学习的轻音乐""90年代经典情歌""婚礼暖场歌曲""动漫神曲")。
   - 适合按歌手(mode=artists): 需求指向歌手/乐团/流派代表人物(如"华语经典男歌手""日本City Pop代表""周杰伦风格的歌")。
2. 按判断结果生成 10-15 个搜索词(mode=songs 时为歌曲名, mode=artists 时为歌手名)。
3. 命中率优先: 只选主流、高知名度、各大音乐平台大概率都能搜到的歌曲/歌手(华语流行经典、知名歌手代表作、大众熟知的金曲)。务必避开冷门、独立、小众、地下、过于新或过于偏门的作品——这些在音源里搜不到, 会让用户失望。
4. 给一个贴切、有吸引力的中文歌单名(≤12字)。

严格返回 JSON: {"mode":"songs"|"artists","items":["...","..."],"playlistName":"歌单名"}`

// ============ AI 协助建歌单 - 过滤候选（按用户需求筛，非单纯"大众认可"）============
// 占位符: {{candidates}} 候选列表; {{userPrompt}} 用户需求
// 返回: {"suggestions":[{"index":序号,"action":"keep"|"remove","reason":"≤20字原因"}]}
export const DEFAULT_PROMPT_AI_PLAYLIST_FILTER = `你是音乐曲库编辑。用户想创建一份歌单, 请根据用户的需求从下列候选歌曲中挑出符合的版本。

用户需求: {{userPrompt}}

候选(序号. 歌名 | 演唱者 | 专辑):
{{candidates}}

规则:
1. 优先保留符合用户需求的歌曲——按需求里的主题、风格、情绪、场景、年代、语言等判断。
2. 在符合需求的前提下, 保留大众认可、传唱度高的好版本（经典原唱录音室版优先, 公认经典翻唱/官方经典 remix 可保留）。
3. 排除 live/现场版、伴奏/纯音乐/铃声/8bit、粗制 DJ 舞曲版、群星/大合唱/晚会、蹭歌名同名异曲——除非用户需求明确要这类（如"纯音乐""现场版""伴奏""remix"）。
4. 同名重复只留最经典的 1 个。
5. 对每条候选给出 keep（符合需求, 建议加入）或 remove（不符合或劣质）及简短原因。

严格返回 JSON: {"suggestions":[{"index":序号,"action":"keep"|"remove","reason":"≤20字原因"}]}`
